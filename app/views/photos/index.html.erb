<h3>Imgur Photos</h3>
<div class="imagelist">
  <% @photos.each_with_index do |p, i| %>
  <div class="image">
    <%= link_to image_tag("#{p.url}", {:class => "main_page_img"}) %>

    Votes: <%= "#{p.votes.count}" %>

    <% if (i+1) % 4 == 0 %>
      </br>
    <% end %>
  </div>
  <% end %>
<div>
